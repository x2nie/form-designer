// export function debounce(func, delay) {
//     let timeoutId;
//     return function (...args) {
//         clearTimeout(timeoutId); // Reset timer
//         timeoutId = setTimeout(() => {
//             func.apply(this, args); // Jalankan fungsi asli
//         }, delay);
//     };
// }
