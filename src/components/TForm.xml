<templates>

    <t t-name="TForm" t-call="TComponent">
        <div class="title-bar" t-ref="titlebar">
            <div class="title-bar-text">Another window with contents</div>
            <div class="title-bar-controls">
                <button aria-label="Minimize"></button>
                <button aria-label="Maximize"></button>
                <button aria-label="Close"></button>
            </div>
        </div>
        <div class="window-body has-space0 root-designing-component">
            <t t-raw="0"/>
            <t t-call="TComponent.children"/>
        </div>
    </t>

</templates>